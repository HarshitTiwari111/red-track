{
  "name": "kap-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "KAP Tracker - self-hosted ad & affiliate tracking platform (MERN)",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c cyan,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "seed": "npm --prefix server run seed",
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop kap-tracker",
    "restart": "pm2 restart kap-tracker",
    "logs": "pm2 logs kap-tracker",
    "serve": "node server/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "pm2": "^5.4.3"
  }
}
